# Gap analysis

You can optionally run this one first, before any builder skill. Ingests all available context (HubSpot, Gong, Gmail, web research, uploaded documents) and outputs a structured JSON brief that tells the downstream builder skills *what* to build and *why*: which workflows to demo, the business framing, and the technical requirements.
